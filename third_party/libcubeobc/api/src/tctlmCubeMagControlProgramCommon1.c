/************************************************************************************
 * This file was auto-generated by CIDEA                           .                *
 * Please do not modify the contents of this file manually.                         *
 ***********************************************************************************/

#include <cubeObc/cubeObc_typeDef.h>
#include <cubeObc/cubeObc_errorDef.h>
#include <cubeObc/cubeObc_tctlmCommsMasterSvc.h>
#include "tctlmCubeMagControlProgramCommon1.h"

/*******************************************************************************
 ***************************  GLOBAL MACROS   **********************************
 ******************************************************************************/

/*******************************************************************************
 **************************   GLOBAL FUNCTIONS   *******************************
 ******************************************************************************/

 /* SET functions */

ErrorCode tctlmCubeMagControlProgramCommon1_setConfig(TypeDef_TctlmEndpoint *endpoint, TctlmCubeMagControlProgramCommon1_Config *setVal)
{
    ErrorCode result = CUBEOBC_ERROR_OK;
    TctlmCommsMasterSvc_Endpoint target;
    U8 *buffer;
    U32 bufferSize;
    U32 bufferSizeUsed;

    ZERO_VAR(target);

    target.id = 60;
    MEMCPY((U8 *)&target.endpoint, (U8 *)endpoint, sizeof(TypeDef_TctlmEndpoint));

    buffer = cubeObc_tctlmCommsMasterSvc_buffer(&target);
    bufferSize = cubeObc_tctlmCommsMasterSvc_bufferSize(&target);

    if (bufferSize < 4)
    {
        result = CUBEOBC_ERROR_SIZEL;
    }

    if (result == CUBEOBC_ERROR_OK)
    {
        U8 prepare0 = ((setVal->primaryMagnetometerPref & 0x0F) << 0)
                    | ((setVal->primaryMagnetometerCurr & 0x0F) << 4);
        MEMCPY(&buffer[0], &prepare0, sizeof(U8));
        /*************************/
        MEMCPY(&buffer[1], &setVal->deployTimeout, sizeof(U16));
        /*************************/
        U8 prepare3 = ((setVal->primaryAutoSelect & 0x01) << 0);
        MEMCPY(&buffer[3], &prepare3, sizeof(U8));
        /*************************/
        bufferSizeUsed = 4;
        result = cubeObc_tctlmCommsMasterSvc_sendReceive(&target, &bufferSizeUsed);
    }

    return result;
}

ErrorCode tctlmCubeMagControlProgramCommon1_setMmcConfig(TypeDef_TctlmEndpoint *endpoint, TctlmCubeMagControlProgramCommon1_MmcConfig *setVal)
{
    ErrorCode result = CUBEOBC_ERROR_OK;
    TctlmCommsMasterSvc_Endpoint target;
    U8 *buffer;
    U32 bufferSize;
    U32 bufferSizeUsed;

    ZERO_VAR(target);

    target.id = 61;
    MEMCPY((U8 *)&target.endpoint, (U8 *)endpoint, sizeof(TypeDef_TctlmEndpoint));

    buffer = cubeObc_tctlmCommsMasterSvc_buffer(&target);
    bufferSize = cubeObc_tctlmCommsMasterSvc_bufferSize(&target);

    if (bufferSize < 12)
    {
        result = CUBEOBC_ERROR_SIZEL;
    }

    if (result == CUBEOBC_ERROR_OK)
    {
        U8 prepare0 = ((setVal->autoSample & 0x01) << 0)
                    | ((setVal->autoSamplePeriod & 0x7F) << 1);
        MEMCPY(&buffer[0], &prepare0, sizeof(U8));
        /*************************/
        U8 prepare1 = ((setVal->sampleFilterType & 0x0F) << 0)
                    | ((setVal->sampleFilterOrder & 0x0F) << 4);
        MEMCPY(&buffer[1], &prepare1, sizeof(U8));
        /*************************/
        MEMCPY(&buffer[2], &setVal->sampleFilterDepth, sizeof(U8));
        /*************************/
        U8 prepare3 = ((setVal->singleSampleTime & 0x1F) << 0)
                    | ((setVal->sampleBusyAction & 0x07) << 5);
        MEMCPY(&buffer[3], &prepare3, sizeof(U8));
        /*************************/
        MEMCPY(&buffer[4], &setVal->maxErrorsMag, sizeof(U32));
        /*************************/
        MEMCPY(&buffer[8], &setVal->maxErrorsTmp, sizeof(U32));
        /*************************/
        bufferSizeUsed = 12;
        result = cubeObc_tctlmCommsMasterSvc_sendReceive(&target, &bufferSizeUsed);
    }

    return result;
}

ErrorCode tctlmCubeMagControlProgramCommon1_setSamplePrimary(TypeDef_TctlmEndpoint *endpoint)
{
    ErrorCode result = CUBEOBC_ERROR_OK;
    TctlmCommsMasterSvc_Endpoint target;
    U32 bufferSizeUsed;

    ZERO_VAR(target);

    target.id = 67;
    MEMCPY((U8 *)&target.endpoint, (U8 *)endpoint, sizeof(TypeDef_TctlmEndpoint));

    if (result == CUBEOBC_ERROR_OK)
    {
        bufferSizeUsed = 0;
        result = cubeObc_tctlmCommsMasterSvc_sendReceive(&target, &bufferSizeUsed);
    }

    return result;
}

ErrorCode tctlmCubeMagControlProgramCommon1_setMmcCalibration(TypeDef_TctlmEndpoint *endpoint, TctlmCubeMagControlProgramCommon1_MmcCalibration *setVal)
{
    ErrorCode result = CUBEOBC_ERROR_OK;
    TctlmCommsMasterSvc_Endpoint target;
    U8 *buffer;
    U32 bufferSize;
    U32 bufferSizeUsed;

    ZERO_VAR(target);

    target.id = 68;
    MEMCPY((U8 *)&target.endpoint, (U8 *)endpoint, sizeof(TypeDef_TctlmEndpoint));

    buffer = cubeObc_tctlmCommsMasterSvc_buffer(&target);
    bufferSize = cubeObc_tctlmCommsMasterSvc_bufferSize(&target);

    if (bufferSize < 48)
    {
        result = CUBEOBC_ERROR_SIZEL;
    }

    if (result == CUBEOBC_ERROR_OK)
    {
        MEMCPY(&buffer[0], &setVal->xd1, sizeof(F32));
        /*************************/
        MEMCPY(&buffer[4], &setVal->xd2, sizeof(F32));
        /*************************/
        MEMCPY(&buffer[8], &setVal->xs1, sizeof(F32));
        /*************************/
        MEMCPY(&buffer[12], &setVal->xs2, sizeof(F32));
        /*************************/
        MEMCPY(&buffer[16], &setVal->yd1, sizeof(F32));
        /*************************/
        MEMCPY(&buffer[20], &setVal->yd2, sizeof(F32));
        /*************************/
        MEMCPY(&buffer[24], &setVal->ys1, sizeof(F32));
        /*************************/
        MEMCPY(&buffer[28], &setVal->ys2, sizeof(F32));
        /*************************/
        MEMCPY(&buffer[32], &setVal->zd1, sizeof(F32));
        /*************************/
        MEMCPY(&buffer[36], &setVal->zd2, sizeof(F32));
        /*************************/
        MEMCPY(&buffer[40], &setVal->zs1, sizeof(F32));
        /*************************/
        MEMCPY(&buffer[44], &setVal->zs2, sizeof(F32));
        /*************************/
        bufferSizeUsed = 48;
        result = cubeObc_tctlmCommsMasterSvc_sendReceive(&target, &bufferSizeUsed);
    }

    return result;
}


 /* GET functions */

ErrorCode tctlmCubeMagControlProgramCommon1_getMmcCalibration(TypeDef_TctlmEndpoint *endpoint, TctlmCubeMagControlProgramCommon1_MmcCalibration *returnVal)
{
    ErrorCode result = CUBEOBC_ERROR_OK;
    TctlmCommsMasterSvc_Endpoint target;
    U8 *buffer;
    U32 bufferSize;
    U32 bufferSizeUsed;

    ZERO_VAR(target);

    target.id = 185;
    MEMCPY((U8 *)&target.endpoint, (U8 *)endpoint, sizeof(TypeDef_TctlmEndpoint));

    buffer = cubeObc_tctlmCommsMasterSvc_buffer(&target);
    bufferSize = cubeObc_tctlmCommsMasterSvc_bufferSize(&target);

    if (bufferSize < 48)
    {
        result = CUBEOBC_ERROR_SIZEL;
    }

    if (result == CUBEOBC_ERROR_OK)
    {
        bufferSizeUsed = 0;
        result = cubeObc_tctlmCommsMasterSvc_sendReceive(&target, &bufferSizeUsed);
    }

    if (result == CUBEOBC_ERROR_OK)
    {
        if (bufferSizeUsed != 48)
        {
            result = CUBEOBC_ERROR_TLM_SIZE;
        }
    }

    if (result == CUBEOBC_ERROR_OK)
    {
        F32 prepare0;
        MEMCPY(&prepare0, &buffer[0], sizeof(F32));
        if ((prepare0 < -100000) || (prepare0 > 100000))
        {
            result = CUBEOBC_ERROR_PARAM;
        }
        returnVal->xd1 = prepare0;
    }
    /*************************/
    if (result == CUBEOBC_ERROR_OK)
    {
        F32 prepare1;
        MEMCPY(&prepare1, &buffer[4], sizeof(F32));
        if ((prepare1 < -100000) || (prepare1 > 100000))
        {
            result = CUBEOBC_ERROR_PARAM;
        }
        returnVal->xd2 = prepare1;
    }
    /*************************/
    if (result == CUBEOBC_ERROR_OK)
    {
        F32 prepare2;
        MEMCPY(&prepare2, &buffer[8], sizeof(F32));
        if ((prepare2 < -100000) || (prepare2 > 100000))
        {
            result = CUBEOBC_ERROR_PARAM;
        }
        returnVal->xs1 = prepare2;
    }
    /*************************/
    if (result == CUBEOBC_ERROR_OK)
    {
        F32 prepare3;
        MEMCPY(&prepare3, &buffer[12], sizeof(F32));
        if ((prepare3 < -100000) || (prepare3 > 100000))
        {
            result = CUBEOBC_ERROR_PARAM;
        }
        returnVal->xs2 = prepare3;
    }
    /*************************/
    if (result == CUBEOBC_ERROR_OK)
    {
        F32 prepare4;
        MEMCPY(&prepare4, &buffer[16], sizeof(F32));
        if ((prepare4 < -100000) || (prepare4 > 100000))
        {
            result = CUBEOBC_ERROR_PARAM;
        }
        returnVal->yd1 = prepare4;
    }
    /*************************/
    if (result == CUBEOBC_ERROR_OK)
    {
        F32 prepare5;
        MEMCPY(&prepare5, &buffer[20], sizeof(F32));
        if ((prepare5 < -100000) || (prepare5 > 100000))
        {
            result = CUBEOBC_ERROR_PARAM;
        }
        returnVal->yd2 = prepare5;
    }
    /*************************/
    if (result == CUBEOBC_ERROR_OK)
    {
        F32 prepare6;
        MEMCPY(&prepare6, &buffer[24], sizeof(F32));
        if ((prepare6 < -100000) || (prepare6 > 100000))
        {
            result = CUBEOBC_ERROR_PARAM;
        }
        returnVal->ys1 = prepare6;
    }
    /*************************/
    if (result == CUBEOBC_ERROR_OK)
    {
        F32 prepare7;
        MEMCPY(&prepare7, &buffer[28], sizeof(F32));
        if ((prepare7 < -100000) || (prepare7 > 100000))
        {
            result = CUBEOBC_ERROR_PARAM;
        }
        returnVal->ys2 = prepare7;
    }
    /*************************/
    if (result == CUBEOBC_ERROR_OK)
    {
        F32 prepare8;
        MEMCPY(&prepare8, &buffer[32], sizeof(F32));
        if ((prepare8 < -100000) || (prepare8 > 100000))
        {
            result = CUBEOBC_ERROR_PARAM;
        }
        returnVal->zd1 = prepare8;
    }
    /*************************/
    if (result == CUBEOBC_ERROR_OK)
    {
        F32 prepare9;
        MEMCPY(&prepare9, &buffer[36], sizeof(F32));
        if ((prepare9 < -100000) || (prepare9 > 100000))
        {
            result = CUBEOBC_ERROR_PARAM;
        }
        returnVal->zd2 = prepare9;
    }
    /*************************/
    if (result == CUBEOBC_ERROR_OK)
    {
        F32 prepare10;
        MEMCPY(&prepare10, &buffer[40], sizeof(F32));
        if ((prepare10 < -100000) || (prepare10 > 100000))
        {
            result = CUBEOBC_ERROR_PARAM;
        }
        returnVal->zs1 = prepare10;
    }
    /*************************/
    if (result == CUBEOBC_ERROR_OK)
    {
        F32 prepare11;
        MEMCPY(&prepare11, &buffer[44], sizeof(F32));
        if ((prepare11 < -100000) || (prepare11 > 100000))
        {
            result = CUBEOBC_ERROR_PARAM;
        }
        returnVal->zs2 = prepare11;
    }
    /*************************/

    return result;
}

ErrorCode tctlmCubeMagControlProgramCommon1_getConfig(TypeDef_TctlmEndpoint *endpoint, TctlmCubeMagControlProgramCommon1_Config *returnVal)
{
    ErrorCode result = CUBEOBC_ERROR_OK;
    TctlmCommsMasterSvc_Endpoint target;
    U8 *buffer;
    U32 bufferSize;
    U32 bufferSizeUsed;

    ZERO_VAR(target);

    target.id = 186;
    MEMCPY((U8 *)&target.endpoint, (U8 *)endpoint, sizeof(TypeDef_TctlmEndpoint));

    buffer = cubeObc_tctlmCommsMasterSvc_buffer(&target);
    bufferSize = cubeObc_tctlmCommsMasterSvc_bufferSize(&target);

    if (bufferSize < 4)
    {
        result = CUBEOBC_ERROR_SIZEL;
    }

    if (result == CUBEOBC_ERROR_OK)
    {
        bufferSizeUsed = 0;
        result = cubeObc_tctlmCommsMasterSvc_sendReceive(&target, &bufferSizeUsed);
    }

    if (result == CUBEOBC_ERROR_OK)
    {
        if (bufferSizeUsed != 4)
        {
            result = CUBEOBC_ERROR_TLM_SIZE;
        }
    }

    if (result == CUBEOBC_ERROR_OK)
    {
        U8 prepare0;
        MEMCPY(&prepare0, &buffer[0], sizeof(U8));
        returnVal->primaryMagnetometerPref = (TctlmCubeMagControlProgramCommonEnums_MagType)((prepare0 >> 0) & 0x0F);
        if ((U8)returnVal->primaryMagnetometerPref > 1)
        {
            result = CUBEOBC_ERROR_PARAM;
        }
    }
    /*************************/
    if (result == CUBEOBC_ERROR_OK)
    {
        U8 prepare1;
        MEMCPY(&prepare1, &buffer[0], sizeof(U8));
        returnVal->primaryMagnetometerCurr = (TctlmCubeMagControlProgramCommonEnums_MagType)((prepare1 >> 4) & 0x0F);
        if ((U8)returnVal->primaryMagnetometerCurr > 1)
        {
            result = CUBEOBC_ERROR_PARAM;
        }
    }
    /*************************/
    if (result == CUBEOBC_ERROR_OK)
    {
        U16 prepare2;
        MEMCPY(&prepare2, &buffer[1], sizeof(U16));
        if ((prepare2 < 2000) || (prepare2 > 30000))
        {
            result = CUBEOBC_ERROR_PARAM;
        }
        returnVal->deployTimeout = prepare2;
    }
    /*************************/
    if (result == CUBEOBC_ERROR_OK)
    {
        U8 prepare3;
        MEMCPY(&prepare3, &buffer[3], sizeof(U8));
        returnVal->primaryAutoSelect = (Boolean)((prepare3 >> 0) & 0x01);
    }
    /*************************/

    return result;
}

ErrorCode tctlmCubeMagControlProgramCommon1_getPrimaryMagMeasurementRaw(TypeDef_TctlmEndpoint *endpoint, TctlmCubeMagControlProgramCommon1_PrimaryMagMeasurementRaw *returnVal)
{
    ErrorCode result = CUBEOBC_ERROR_OK;
    TctlmCommsMasterSvc_Endpoint target;
    U8 *buffer;
    U32 bufferSize;
    U32 bufferSizeUsed;

    ZERO_VAR(target);

    target.id = 187;
    MEMCPY((U8 *)&target.endpoint, (U8 *)endpoint, sizeof(TypeDef_TctlmEndpoint));

    buffer = cubeObc_tctlmCommsMasterSvc_buffer(&target);
    bufferSize = cubeObc_tctlmCommsMasterSvc_bufferSize(&target);

    if (bufferSize < 13)
    {
        result = CUBEOBC_ERROR_SIZEL;
    }

    if (result == CUBEOBC_ERROR_OK)
    {
        bufferSizeUsed = 0;
        result = cubeObc_tctlmCommsMasterSvc_sendReceive(&target, &bufferSizeUsed);
    }

    if (result == CUBEOBC_ERROR_OK)
    {
        if (bufferSizeUsed != 13)
        {
            result = CUBEOBC_ERROR_TLM_SIZE;
        }
    }

    if (result == CUBEOBC_ERROR_OK)
    {
        F32 prepare0;
        MEMCPY(&prepare0, &buffer[0], sizeof(F32));
        if ((prepare0 < -800000) || (prepare0 > 800000))
        {
            result = CUBEOBC_ERROR_PARAM;
        }
        returnVal->x_axis = prepare0;
    }
    /*************************/
    if (result == CUBEOBC_ERROR_OK)
    {
        F32 prepare1;
        MEMCPY(&prepare1, &buffer[4], sizeof(F32));
        if ((prepare1 < -800000) || (prepare1 > 800000))
        {
            result = CUBEOBC_ERROR_PARAM;
        }
        returnVal->y_axis = prepare1;
    }
    /*************************/
    if (result == CUBEOBC_ERROR_OK)
    {
        F32 prepare2;
        MEMCPY(&prepare2, &buffer[8], sizeof(F32));
        if ((prepare2 < -800000) || (prepare2 > 800000))
        {
            result = CUBEOBC_ERROR_PARAM;
        }
        returnVal->z_axis = prepare2;
    }
    /*************************/
    if (result == CUBEOBC_ERROR_OK)
    {
        U8 prepare3;
        MEMCPY(&prepare3, &buffer[12], sizeof(U8));
        returnVal->dataValid = (Boolean)((prepare3 >> 0) & 0x01);
    }
    /*************************/

    return result;
}

ErrorCode tctlmCubeMagControlProgramCommon1_getState(TypeDef_TctlmEndpoint *endpoint, TctlmCubeMagControlProgramCommon1_State *returnVal)
{
    ErrorCode result = CUBEOBC_ERROR_OK;
    TctlmCommsMasterSvc_Endpoint target;
    U8 *buffer;
    U32 bufferSize;
    U32 bufferSizeUsed;

    ZERO_VAR(target);

    target.id = 188;
    MEMCPY((U8 *)&target.endpoint, (U8 *)endpoint, sizeof(TypeDef_TctlmEndpoint));

    buffer = cubeObc_tctlmCommsMasterSvc_buffer(&target);
    bufferSize = cubeObc_tctlmCommsMasterSvc_bufferSize(&target);

    if (bufferSize < 3)
    {
        result = CUBEOBC_ERROR_SIZEL;
    }

    if (result == CUBEOBC_ERROR_OK)
    {
        bufferSizeUsed = 0;
        result = cubeObc_tctlmCommsMasterSvc_sendReceive(&target, &bufferSizeUsed);
    }

    if (result == CUBEOBC_ERROR_OK)
    {
        if (bufferSizeUsed != 3)
        {
            result = CUBEOBC_ERROR_TLM_SIZE;
        }
    }

    if (result == CUBEOBC_ERROR_OK)
    {
        U8 prepare0;
        MEMCPY(&prepare0, &buffer[0], sizeof(U8));
        returnVal->magState = (TctlmCubeMagControlProgramCommonEnums_MagState)((prepare0 >> 0) & 0xFF);
        if ((U8)returnVal->magState > 3)
        {
            result = CUBEOBC_ERROR_PARAM;
        }
    }
    /*************************/
    if (result == CUBEOBC_ERROR_OK)
    {
        U8 prepare1;
        MEMCPY(&prepare1, &buffer[1], sizeof(U8));
        returnVal->primaryMagState = (TctlmCubeMagControlProgramCommonEnums_MntState)((prepare1 >> 0) & 0xFF);
        if ((U8)returnVal->primaryMagState > 5)
        {
            result = CUBEOBC_ERROR_PARAM;
        }
    }
    /*************************/
    if (result == CUBEOBC_ERROR_OK)
    {
        U8 prepare2;
        MEMCPY(&prepare2, &buffer[2], sizeof(U8));
        returnVal->redMagState = (TctlmCubeMagControlProgramCommonEnums_MntState)((prepare2 >> 0) & 0xFF);
        if ((U8)returnVal->redMagState > 5)
        {
            result = CUBEOBC_ERROR_PARAM;
        }
    }
    /*************************/

    return result;
}

ErrorCode tctlmCubeMagControlProgramCommon1_getMmcConfig(TypeDef_TctlmEndpoint *endpoint, TctlmCubeMagControlProgramCommon1_MmcConfig *returnVal)
{
    ErrorCode result = CUBEOBC_ERROR_OK;
    TctlmCommsMasterSvc_Endpoint target;
    U8 *buffer;
    U32 bufferSize;
    U32 bufferSizeUsed;

    ZERO_VAR(target);

    target.id = 196;
    MEMCPY((U8 *)&target.endpoint, (U8 *)endpoint, sizeof(TypeDef_TctlmEndpoint));

    buffer = cubeObc_tctlmCommsMasterSvc_buffer(&target);
    bufferSize = cubeObc_tctlmCommsMasterSvc_bufferSize(&target);

    if (bufferSize < 12)
    {
        result = CUBEOBC_ERROR_SIZEL;
    }

    if (result == CUBEOBC_ERROR_OK)
    {
        bufferSizeUsed = 0;
        result = cubeObc_tctlmCommsMasterSvc_sendReceive(&target, &bufferSizeUsed);
    }

    if (result == CUBEOBC_ERROR_OK)
    {
        if (bufferSizeUsed != 12)
        {
            result = CUBEOBC_ERROR_TLM_SIZE;
        }
    }

    if (result == CUBEOBC_ERROR_OK)
    {
        U8 prepare0;
        MEMCPY(&prepare0, &buffer[0], sizeof(U8));
        returnVal->autoSample = (Boolean)((prepare0 >> 0) & 0x01);
    }
    /*************************/
    if (result == CUBEOBC_ERROR_OK)
    {
        U8 prepare1;
        MEMCPY(&prepare1, &buffer[0], sizeof(U8));
        returnVal->autoSamplePeriod = (TctlmCubeMagControlProgramCommonEnums_SamplePeriod)((prepare1 >> 1) & 0x7F);
        if ((U8)returnVal->autoSamplePeriod > 9)
        {
            result = CUBEOBC_ERROR_PARAM;
        }
    }
    /*************************/
    if (result == CUBEOBC_ERROR_OK)
    {
        U8 prepare2;
        MEMCPY(&prepare2, &buffer[1], sizeof(U8));
        returnVal->sampleFilterType = (TctlmCubeMagControlProgramCommonEnums_FilterType)((prepare2 >> 0) & 0x0F);
        if ((U8)returnVal->sampleFilterType > 1)
        {
            result = CUBEOBC_ERROR_PARAM;
        }
    }
    /*************************/
    if (result == CUBEOBC_ERROR_OK)
    {
        U8 prepare3;
        MEMCPY(&prepare3, &buffer[1], sizeof(U8));
        returnVal->sampleFilterOrder = (TctlmCubeMagControlProgramCommonEnums_FilterOrder)((prepare3 >> 4) & 0x0F);
        if ((U8)returnVal->sampleFilterOrder > 3)
        {
            result = CUBEOBC_ERROR_PARAM;
        }
    }
    /*************************/
    if (result == CUBEOBC_ERROR_OK)
    {
        MEMCPY(&returnVal->sampleFilterDepth, &buffer[2], sizeof(U8));
    }
    /*************************/
    if (result == CUBEOBC_ERROR_OK)
    {
        U8 prepare5;
        MEMCPY(&prepare5, &buffer[3], sizeof(U8));
        returnVal->singleSampleTime = (TctlmCubeMagControlProgramCommonEnums_SampleDuration)((prepare5 >> 0) & 0x1F);
        if (((U8)returnVal->singleSampleTime < 4) ||
            ((U8)returnVal->singleSampleTime > 15))
        {
            result = CUBEOBC_ERROR_PARAM;
        }
    }
    /*************************/
    if (result == CUBEOBC_ERROR_OK)
    {
        U8 prepare6;
        MEMCPY(&prepare6, &buffer[3], sizeof(U8));
        returnVal->sampleBusyAction = (TctlmCubeMagControlProgramCommonEnums_SampleBusy)((prepare6 >> 5) & 0x07);
        if ((U8)returnVal->sampleBusyAction > 1)
        {
            result = CUBEOBC_ERROR_PARAM;
        }
    }
    /*************************/
    if (result == CUBEOBC_ERROR_OK)
    {
        MEMCPY(&returnVal->maxErrorsMag, &buffer[4], sizeof(U32));
    }
    /*************************/
    if (result == CUBEOBC_ERROR_OK)
    {
        MEMCPY(&returnVal->maxErrorsTmp, &buffer[8], sizeof(U32));
    }
    /*************************/

    return result;
}

ErrorCode tctlmCubeMagControlProgramCommon1_getPrimaryMagMeasurement(TypeDef_TctlmEndpoint *endpoint, TctlmCubeMagControlProgramCommon1_PrimaryMagMeasurement *returnVal)
{
    ErrorCode result = CUBEOBC_ERROR_OK;
    TctlmCommsMasterSvc_Endpoint target;
    U8 *buffer;
    U32 bufferSize;
    U32 bufferSizeUsed;

    ZERO_VAR(target);

    target.id = 197;
    MEMCPY((U8 *)&target.endpoint, (U8 *)endpoint, sizeof(TypeDef_TctlmEndpoint));

    buffer = cubeObc_tctlmCommsMasterSvc_buffer(&target);
    bufferSize = cubeObc_tctlmCommsMasterSvc_bufferSize(&target);

    if (bufferSize < 13)
    {
        result = CUBEOBC_ERROR_SIZEL;
    }

    if (result == CUBEOBC_ERROR_OK)
    {
        bufferSizeUsed = 0;
        result = cubeObc_tctlmCommsMasterSvc_sendReceive(&target, &bufferSizeUsed);
    }

    if (result == CUBEOBC_ERROR_OK)
    {
        if (bufferSizeUsed != 13)
        {
            result = CUBEOBC_ERROR_TLM_SIZE;
        }
    }

    if (result == CUBEOBC_ERROR_OK)
    {
        F32 prepare0;
        MEMCPY(&prepare0, &buffer[0], sizeof(F32));
        if ((prepare0 < -800000) || (prepare0 > 800000))
        {
            result = CUBEOBC_ERROR_PARAM;
        }
        returnVal->x_axis = prepare0;
    }
    /*************************/
    if (result == CUBEOBC_ERROR_OK)
    {
        F32 prepare1;
        MEMCPY(&prepare1, &buffer[4], sizeof(F32));
        if ((prepare1 < -800000) || (prepare1 > 800000))
        {
            result = CUBEOBC_ERROR_PARAM;
        }
        returnVal->y_axis = prepare1;
    }
    /*************************/
    if (result == CUBEOBC_ERROR_OK)
    {
        F32 prepare2;
        MEMCPY(&prepare2, &buffer[8], sizeof(F32));
        if ((prepare2 < -800000) || (prepare2 > 800000))
        {
            result = CUBEOBC_ERROR_PARAM;
        }
        returnVal->z_axis = prepare2;
    }
    /*************************/
    if (result == CUBEOBC_ERROR_OK)
    {
        U8 prepare3;
        MEMCPY(&prepare3, &buffer[12], sizeof(U8));
        returnVal->dataValid = (Boolean)((prepare3 >> 0) & 0x01);
    }
    /*************************/

    return result;
}

ErrorCode tctlmCubeMagControlProgramCommon1_getPrimaryMagTemp(TypeDef_TctlmEndpoint *endpoint, F32 *returnVal)
{
    ErrorCode result = CUBEOBC_ERROR_OK;
    TctlmCommsMasterSvc_Endpoint target;
    U8 *buffer;
    U32 bufferSize;
    U32 bufferSizeUsed;

    ZERO_VAR(target);

    target.id = 198;
    MEMCPY((U8 *)&target.endpoint, (U8 *)endpoint, sizeof(TypeDef_TctlmEndpoint));

    buffer = cubeObc_tctlmCommsMasterSvc_buffer(&target);
    bufferSize = cubeObc_tctlmCommsMasterSvc_bufferSize(&target);

    if (bufferSize < 4)
    {
        result = CUBEOBC_ERROR_SIZEL;
    }

    if (result == CUBEOBC_ERROR_OK)
    {
        bufferSizeUsed = 0;
        result = cubeObc_tctlmCommsMasterSvc_sendReceive(&target, &bufferSizeUsed);
    }

    if (result == CUBEOBC_ERROR_OK)
    {
        if (bufferSizeUsed != 4)
        {
            result = CUBEOBC_ERROR_TLM_SIZE;
        }
    }

    if (result == CUBEOBC_ERROR_OK)
    {
        F32 prepare0;
        MEMCPY(&prepare0, &buffer[0], sizeof(F32));
        if ((prepare0 < -75) || (prepare0 > 150))
        {
            result = CUBEOBC_ERROR_PARAM;
        }
        *returnVal = prepare0;
    }
    /*************************/

    return result;
}

ErrorCode tctlmCubeMagControlProgramCommon1_getPrimaryMagDiagnostics(TypeDef_TctlmEndpoint *endpoint, TctlmCubeMagControlProgramCommon1_PrimaryMagDiagnostics *returnVal)
{
    ErrorCode result = CUBEOBC_ERROR_OK;
    TctlmCommsMasterSvc_Endpoint target;
    U8 *buffer;
    U32 bufferSize;
    U32 bufferSizeUsed;

    ZERO_VAR(target);

    target.id = 199;
    MEMCPY((U8 *)&target.endpoint, (U8 *)endpoint, sizeof(TypeDef_TctlmEndpoint));

    buffer = cubeObc_tctlmCommsMasterSvc_buffer(&target);
    bufferSize = cubeObc_tctlmCommsMasterSvc_bufferSize(&target);

    if (bufferSize < 16)
    {
        result = CUBEOBC_ERROR_SIZEL;
    }

    if (result == CUBEOBC_ERROR_OK)
    {
        bufferSizeUsed = 0;
        result = cubeObc_tctlmCommsMasterSvc_sendReceive(&target, &bufferSizeUsed);
    }

    if (result == CUBEOBC_ERROR_OK)
    {
        if (bufferSizeUsed != 16)
        {
            result = CUBEOBC_ERROR_TLM_SIZE;
        }
    }

    if (result == CUBEOBC_ERROR_OK)
    {
        MEMCPY(&returnVal->lastErrorMag, &buffer[0], sizeof(U32));
    }
    /*************************/
    if (result == CUBEOBC_ERROR_OK)
    {
        MEMCPY(&returnVal->errorCountMag, &buffer[4], sizeof(U32));
    }
    /*************************/
    if (result == CUBEOBC_ERROR_OK)
    {
        MEMCPY(&returnVal->lastErrorTmp, &buffer[8], sizeof(U32));
    }
    /*************************/
    if (result == CUBEOBC_ERROR_OK)
    {
        MEMCPY(&returnVal->errorCountTmp, &buffer[12], sizeof(U32));
    }
    /*************************/

    return result;
}

