/************************************************************************************
 * This file was auto-generated by CIDEA                           .                *
 * Please do not modify the contents of this file manually.                         *
 ***********************************************************************************/

#include <cubeObc/cubeObc_typeDef.h>
#include <cubeObc/cubeObc_errorDef.h>
#include <cubeObc/cubeObc_tctlmCommsMasterSvc.h>
#include "tctlmCubeNodeControlProgramLitefufors1.h"

/*******************************************************************************
 ***************************  GLOBAL MACROS   **********************************
 ******************************************************************************/

/*******************************************************************************
 **************************   GLOBAL FUNCTIONS   *******************************
 ******************************************************************************/

 /* SET functions */

ErrorCode tctlmCubeNodeControlProgramLitefufors1_setUforsPortMap(TypeDef_TctlmEndpoint *endpoint, TctlmCubeNodeControlProgramLitefufors1_UforsPortMap *setVal)
{
    ErrorCode result = CUBEOBC_ERROR_OK;
    TctlmCommsMasterSvc_Endpoint target;
    U8 *buffer;
    U32 bufferSize;
    U32 bufferSizeUsed;

    ZERO_VAR(target);

    target.id = 67;
    MEMCPY((U8 *)&target.endpoint, (U8 *)endpoint, sizeof(TypeDef_TctlmEndpoint));

    buffer = cubeObc_tctlmCommsMasterSvc_buffer(&target);
    bufferSize = cubeObc_tctlmCommsMasterSvc_bufferSize(&target);

    if (bufferSize < 1)
    {
        result = CUBEOBC_ERROR_SIZEL;
    }

    if (result == CUBEOBC_ERROR_OK)
    {
        U8 prepare0 = ((setVal->uforsAxisPort1 & 0x03) << 0)
                    | ((setVal->uforsAxisPort2 & 0x03) << 2)
                    | ((setVal->uforsAxisPort3 & 0x03) << 4)
                    | ((setVal->uforsAxisPort4 & 0x03) << 6);
        MEMCPY(&buffer[0], &prepare0, sizeof(U8));
        /*************************/
        bufferSizeUsed = 1;
        result = cubeObc_tctlmCommsMasterSvc_sendReceive(&target, &bufferSizeUsed);
    }

    return result;
}

ErrorCode tctlmCubeNodeControlProgramLitefufors1_setUforsPower(TypeDef_TctlmEndpoint *endpoint, TctlmCubeNodeControlProgramLitefufors1_UforsPower *setVal)
{
    ErrorCode result = CUBEOBC_ERROR_OK;
    TctlmCommsMasterSvc_Endpoint target;
    U8 *buffer;
    U32 bufferSize;
    U32 bufferSizeUsed;

    ZERO_VAR(target);

    target.id = 68;
    MEMCPY((U8 *)&target.endpoint, (U8 *)endpoint, sizeof(TypeDef_TctlmEndpoint));

    buffer = cubeObc_tctlmCommsMasterSvc_buffer(&target);
    bufferSize = cubeObc_tctlmCommsMasterSvc_bufferSize(&target);

    if (bufferSize < 1)
    {
        result = CUBEOBC_ERROR_SIZEL;
    }

    if (result == CUBEOBC_ERROR_OK)
    {
        U8 prepare0 = ((setVal->powerUforsX & 0x03) << 0)
                    | ((setVal->powerUforsY & 0x03) << 2)
                    | ((setVal->powerUforsZ & 0x03) << 4);
        MEMCPY(&buffer[0], &prepare0, sizeof(U8));
        /*************************/
        bufferSizeUsed = 1;
        result = cubeObc_tctlmCommsMasterSvc_sendReceive(&target, &bufferSizeUsed);
    }

    return result;
}

ErrorCode tctlmCubeNodeControlProgramLitefufors1_setNodeSvcSettings(TypeDef_TctlmEndpoint *endpoint, Boolean setVal)
{
    ErrorCode result = CUBEOBC_ERROR_OK;
    TctlmCommsMasterSvc_Endpoint target;
    U8 *buffer;
    U32 bufferSize;
    U32 bufferSizeUsed;

    ZERO_VAR(target);

    target.id = 69;
    MEMCPY((U8 *)&target.endpoint, (U8 *)endpoint, sizeof(TypeDef_TctlmEndpoint));

    buffer = cubeObc_tctlmCommsMasterSvc_buffer(&target);
    bufferSize = cubeObc_tctlmCommsMasterSvc_bufferSize(&target);

    if (bufferSize < 1)
    {
        result = CUBEOBC_ERROR_SIZEL;
    }

    if (result == CUBEOBC_ERROR_OK)
    {
        U8 prepare0 = ((setVal & 0x01) << 0);
        MEMCPY(&buffer[0], &prepare0, sizeof(U8));
        /*************************/
        bufferSizeUsed = 1;
        result = cubeObc_tctlmCommsMasterSvc_sendReceive(&target, &bufferSizeUsed);
    }

    return result;
}

ErrorCode tctlmCubeNodeControlProgramLitefufors1_setUforsSvcSettings(TypeDef_TctlmEndpoint *endpoint, TctlmCubeNodeControlProgramLitefufors1_UforsSvcSettings *setVal)
{
    ErrorCode result = CUBEOBC_ERROR_OK;
    TctlmCommsMasterSvc_Endpoint target;
    U8 *buffer;
    U32 bufferSize;
    U32 bufferSizeUsed;

    ZERO_VAR(target);

    target.id = 70;
    MEMCPY((U8 *)&target.endpoint, (U8 *)endpoint, sizeof(TypeDef_TctlmEndpoint));

    buffer = cubeObc_tctlmCommsMasterSvc_buffer(&target);
    bufferSize = cubeObc_tctlmCommsMasterSvc_bufferSize(&target);

    if (bufferSize < 7)
    {
        result = CUBEOBC_ERROR_SIZEL;
    }

    if (result == CUBEOBC_ERROR_OK)
    {
        MEMCPY(&buffer[0], &setVal->maxReqErrors, sizeof(U32));
        /*************************/
        MEMCPY(&buffer[4], &setVal->sampleFrequency, sizeof(U16));
        /*************************/
        U8 prepare6 = ((setVal->autoSample & 0x01) << 0);
        MEMCPY(&buffer[6], &prepare6, sizeof(U8));
        /*************************/
        bufferSizeUsed = 7;
        result = cubeObc_tctlmCommsMasterSvc_sendReceive(&target, &bufferSizeUsed);
    }

    return result;
}


 /* GET functions */

ErrorCode tctlmCubeNodeControlProgramLitefufors1_getUforsTlmZ(TypeDef_TctlmEndpoint *endpoint, TctlmCubeNodeControlProgramLitefufors1_UforsTlmZ *returnVal)
{
    ErrorCode result = CUBEOBC_ERROR_OK;
    TctlmCommsMasterSvc_Endpoint target;
    U8 *buffer;
    U32 bufferSize;
    U32 bufferSizeUsed;

    ZERO_VAR(target);

    target.id = 188;
    MEMCPY((U8 *)&target.endpoint, (U8 *)endpoint, sizeof(TypeDef_TctlmEndpoint));

    buffer = cubeObc_tctlmCommsMasterSvc_buffer(&target);
    bufferSize = cubeObc_tctlmCommsMasterSvc_bufferSize(&target);

    if (bufferSize < 5)
    {
        result = CUBEOBC_ERROR_SIZEL;
    }

    if (result == CUBEOBC_ERROR_OK)
    {
        bufferSizeUsed = 0;
        result = cubeObc_tctlmCommsMasterSvc_sendReceive(&target, &bufferSizeUsed);
    }

    if (result == CUBEOBC_ERROR_OK)
    {
        if (bufferSizeUsed != 5)
        {
            result = CUBEOBC_ERROR_TLM_SIZE;
        }
    }

    if (result == CUBEOBC_ERROR_OK)
    {
        F32 prepare0;
        MEMCPY(&prepare0, &buffer[0], sizeof(F32));
        if ((prepare0 < -20) || (prepare0 > 20))
        {
            result = CUBEOBC_ERROR_PARAM;
        }
        returnVal->rate = prepare0;
    }
    /*************************/
    if (result == CUBEOBC_ERROR_OK)
    {
        U8 prepare1;
        MEMCPY(&prepare1, &buffer[4], sizeof(U8));
        returnVal->nogo = (Boolean)((prepare1 >> 0) & 0x01);
    }
    /*************************/
    if (result == CUBEOBC_ERROR_OK)
    {
        U8 prepare2;
        MEMCPY(&prepare2, &buffer[4], sizeof(U8));
        returnVal->rstAck = (Boolean)((prepare2 >> 1) & 0x01);
    }
    /*************************/
    if (result == CUBEOBC_ERROR_OK)
    {
        U8 prepare3;
        MEMCPY(&prepare3, &buffer[4], sizeof(U8));
        returnVal->tempWarn = (Boolean)((prepare3 >> 2) & 0x01);
    }
    /*************************/
    if (result == CUBEOBC_ERROR_OK)
    {
        U8 prepare4;
        MEMCPY(&prepare4, &buffer[4], sizeof(U8));
        returnVal->auxControlLoopErr = (Boolean)((prepare4 >> 3) & 0x01);
    }
    /*************************/
    if (result == CUBEOBC_ERROR_OK)
    {
        U8 prepare5;
        MEMCPY(&prepare5, &buffer[4], sizeof(U8));
        returnVal->hardBitErr = (Boolean)((prepare5 >> 4) & 0x01);
    }
    /*************************/
    if (result == CUBEOBC_ERROR_OK)
    {
        U8 prepare6;
        MEMCPY(&prepare6, &buffer[4], sizeof(U8));
        returnVal->measRangeErr = (Boolean)((prepare6 >> 5) & 0x01);
    }
    /*************************/
    if (result == CUBEOBC_ERROR_OK)
    {
        U8 prepare7;
        MEMCPY(&prepare7, &buffer[4], sizeof(U8));
        returnVal->unknwnCmd = (Boolean)((prepare7 >> 6) & 0x01);
    }
    /*************************/

    return result;
}

ErrorCode tctlmCubeNodeControlProgramLitefufors1_getUforsTlmX(TypeDef_TctlmEndpoint *endpoint, TctlmCubeNodeControlProgramLitefufors1_UforsTlmX *returnVal)
{
    ErrorCode result = CUBEOBC_ERROR_OK;
    TctlmCommsMasterSvc_Endpoint target;
    U8 *buffer;
    U32 bufferSize;
    U32 bufferSizeUsed;

    ZERO_VAR(target);

    target.id = 189;
    MEMCPY((U8 *)&target.endpoint, (U8 *)endpoint, sizeof(TypeDef_TctlmEndpoint));

    buffer = cubeObc_tctlmCommsMasterSvc_buffer(&target);
    bufferSize = cubeObc_tctlmCommsMasterSvc_bufferSize(&target);

    if (bufferSize < 5)
    {
        result = CUBEOBC_ERROR_SIZEL;
    }

    if (result == CUBEOBC_ERROR_OK)
    {
        bufferSizeUsed = 0;
        result = cubeObc_tctlmCommsMasterSvc_sendReceive(&target, &bufferSizeUsed);
    }

    if (result == CUBEOBC_ERROR_OK)
    {
        if (bufferSizeUsed != 5)
        {
            result = CUBEOBC_ERROR_TLM_SIZE;
        }
    }

    if (result == CUBEOBC_ERROR_OK)
    {
        F32 prepare0;
        MEMCPY(&prepare0, &buffer[0], sizeof(F32));
        if ((prepare0 < -20) || (prepare0 > 20))
        {
            result = CUBEOBC_ERROR_PARAM;
        }
        returnVal->rate = prepare0;
    }
    /*************************/
    if (result == CUBEOBC_ERROR_OK)
    {
        U8 prepare1;
        MEMCPY(&prepare1, &buffer[4], sizeof(U8));
        returnVal->nogo = (Boolean)((prepare1 >> 0) & 0x01);
    }
    /*************************/
    if (result == CUBEOBC_ERROR_OK)
    {
        U8 prepare2;
        MEMCPY(&prepare2, &buffer[4], sizeof(U8));
        returnVal->rstAck = (Boolean)((prepare2 >> 1) & 0x01);
    }
    /*************************/
    if (result == CUBEOBC_ERROR_OK)
    {
        U8 prepare3;
        MEMCPY(&prepare3, &buffer[4], sizeof(U8));
        returnVal->tempWarn = (Boolean)((prepare3 >> 2) & 0x01);
    }
    /*************************/
    if (result == CUBEOBC_ERROR_OK)
    {
        U8 prepare4;
        MEMCPY(&prepare4, &buffer[4], sizeof(U8));
        returnVal->auxControlLoopErr = (Boolean)((prepare4 >> 3) & 0x01);
    }
    /*************************/
    if (result == CUBEOBC_ERROR_OK)
    {
        U8 prepare5;
        MEMCPY(&prepare5, &buffer[4], sizeof(U8));
        returnVal->hardBitErr = (Boolean)((prepare5 >> 4) & 0x01);
    }
    /*************************/
    if (result == CUBEOBC_ERROR_OK)
    {
        U8 prepare6;
        MEMCPY(&prepare6, &buffer[4], sizeof(U8));
        returnVal->measRangeErr = (Boolean)((prepare6 >> 5) & 0x01);
    }
    /*************************/
    if (result == CUBEOBC_ERROR_OK)
    {
        U8 prepare7;
        MEMCPY(&prepare7, &buffer[4], sizeof(U8));
        returnVal->unknwnCmd = (Boolean)((prepare7 >> 6) & 0x01);
    }
    /*************************/

    return result;
}

ErrorCode tctlmCubeNodeControlProgramLitefufors1_getUforsSvcStatus(TypeDef_TctlmEndpoint *endpoint, TctlmCubeNodeControlProgramLitefufors1_UforsSvcStatus *returnVal)
{
    ErrorCode result = CUBEOBC_ERROR_OK;
    TctlmCommsMasterSvc_Endpoint target;
    U8 *buffer;
    U32 bufferSize;
    U32 bufferSizeUsed;

    ZERO_VAR(target);

    target.id = 190;
    MEMCPY((U8 *)&target.endpoint, (U8 *)endpoint, sizeof(TypeDef_TctlmEndpoint));

    buffer = cubeObc_tctlmCommsMasterSvc_buffer(&target);
    bufferSize = cubeObc_tctlmCommsMasterSvc_bufferSize(&target);

    if (bufferSize < 30)
    {
        result = CUBEOBC_ERROR_SIZEL;
    }

    if (result == CUBEOBC_ERROR_OK)
    {
        bufferSizeUsed = 0;
        result = cubeObc_tctlmCommsMasterSvc_sendReceive(&target, &bufferSizeUsed);
    }

    if (result == CUBEOBC_ERROR_OK)
    {
        if (bufferSizeUsed != 30)
        {
            result = CUBEOBC_ERROR_TLM_SIZE;
        }
    }

    if (result == CUBEOBC_ERROR_OK)
    {
        U8 prepare0;
        MEMCPY(&prepare0, &buffer[0], sizeof(U8));
        returnVal->stateUforsX = (TctlmCubeNodeControlProgramLitefufors1_UforsState)((prepare0 >> 0) & 0xFF);
        if ((U8)returnVal->stateUforsX > 5)
        {
            result = CUBEOBC_ERROR_PARAM;
        }
    }
    /*************************/
    if (result == CUBEOBC_ERROR_OK)
    {
        MEMCPY(&returnVal->failCountUforsX, &buffer[1], sizeof(U32));
    }
    /*************************/
    if (result == CUBEOBC_ERROR_OK)
    {
        MEMCPY(&returnVal->failCountLastSuccessUforsX, &buffer[5], sizeof(U32));
    }
    /*************************/
    if (result == CUBEOBC_ERROR_OK)
    {
        U8 prepare3;
        MEMCPY(&prepare3, &buffer[9], sizeof(U8));
        returnVal->lastReqResultUforsX = (TctlmCubeNodeControlProgramLitefufors1_UforsReqResult)((prepare3 >> 0) & 0xFF);
        if ((U8)returnVal->lastReqResultUforsX > 7)
        {
            result = CUBEOBC_ERROR_PARAM;
        }
    }
    /*************************/
    if (result == CUBEOBC_ERROR_OK)
    {
        U8 prepare4;
        MEMCPY(&prepare4, &buffer[10], sizeof(U8));
        returnVal->stateUforsY = (TctlmCubeNodeControlProgramLitefufors1_UforsState)((prepare4 >> 0) & 0xFF);
        if ((U8)returnVal->stateUforsY > 5)
        {
            result = CUBEOBC_ERROR_PARAM;
        }
    }
    /*************************/
    if (result == CUBEOBC_ERROR_OK)
    {
        MEMCPY(&returnVal->failCountUforsY, &buffer[11], sizeof(U32));
    }
    /*************************/
    if (result == CUBEOBC_ERROR_OK)
    {
        MEMCPY(&returnVal->failCountLastSuccessUforsY, &buffer[15], sizeof(U32));
    }
    /*************************/
    if (result == CUBEOBC_ERROR_OK)
    {
        U8 prepare7;
        MEMCPY(&prepare7, &buffer[19], sizeof(U8));
        returnVal->lastReqResultUforsY = (TctlmCubeNodeControlProgramLitefufors1_UforsReqResult)((prepare7 >> 0) & 0xFF);
        if ((U8)returnVal->lastReqResultUforsY > 7)
        {
            result = CUBEOBC_ERROR_PARAM;
        }
    }
    /*************************/
    if (result == CUBEOBC_ERROR_OK)
    {
        U8 prepare8;
        MEMCPY(&prepare8, &buffer[20], sizeof(U8));
        returnVal->stateUforsZ = (TctlmCubeNodeControlProgramLitefufors1_UforsState)((prepare8 >> 0) & 0xFF);
        if ((U8)returnVal->stateUforsZ > 5)
        {
            result = CUBEOBC_ERROR_PARAM;
        }
    }
    /*************************/
    if (result == CUBEOBC_ERROR_OK)
    {
        MEMCPY(&returnVal->failCountUforsZ, &buffer[21], sizeof(U32));
    }
    /*************************/
    if (result == CUBEOBC_ERROR_OK)
    {
        MEMCPY(&returnVal->failCountLastSuccessUforsZ, &buffer[25], sizeof(U32));
    }
    /*************************/
    if (result == CUBEOBC_ERROR_OK)
    {
        U8 prepare11;
        MEMCPY(&prepare11, &buffer[29], sizeof(U8));
        returnVal->lastReqResultUforsZ = (TctlmCubeNodeControlProgramLitefufors1_UforsReqResult)((prepare11 >> 0) & 0xFF);
        if ((U8)returnVal->lastReqResultUforsZ > 7)
        {
            result = CUBEOBC_ERROR_PARAM;
        }
    }
    /*************************/

    return result;
}

ErrorCode tctlmCubeNodeControlProgramLitefufors1_getUforsSvcSettings(TypeDef_TctlmEndpoint *endpoint, TctlmCubeNodeControlProgramLitefufors1_UforsSvcSettings *returnVal)
{
    ErrorCode result = CUBEOBC_ERROR_OK;
    TctlmCommsMasterSvc_Endpoint target;
    U8 *buffer;
    U32 bufferSize;
    U32 bufferSizeUsed;

    ZERO_VAR(target);

    target.id = 191;
    MEMCPY((U8 *)&target.endpoint, (U8 *)endpoint, sizeof(TypeDef_TctlmEndpoint));

    buffer = cubeObc_tctlmCommsMasterSvc_buffer(&target);
    bufferSize = cubeObc_tctlmCommsMasterSvc_bufferSize(&target);

    if (bufferSize < 7)
    {
        result = CUBEOBC_ERROR_SIZEL;
    }

    if (result == CUBEOBC_ERROR_OK)
    {
        bufferSizeUsed = 0;
        result = cubeObc_tctlmCommsMasterSvc_sendReceive(&target, &bufferSizeUsed);
    }

    if (result == CUBEOBC_ERROR_OK)
    {
        if (bufferSizeUsed != 7)
        {
            result = CUBEOBC_ERROR_TLM_SIZE;
        }
    }

    if (result == CUBEOBC_ERROR_OK)
    {
        MEMCPY(&returnVal->maxReqErrors, &buffer[0], sizeof(U32));
    }
    /*************************/
    if (result == CUBEOBC_ERROR_OK)
    {
        U16 prepare1;
        MEMCPY(&prepare1, &buffer[4], sizeof(U16));
        if ((prepare1 < 5) || (prepare1 > 1000))
        {
            result = CUBEOBC_ERROR_PARAM;
        }
        returnVal->sampleFrequency = prepare1;
    }
    /*************************/
    if (result == CUBEOBC_ERROR_OK)
    {
        U8 prepare2;
        MEMCPY(&prepare2, &buffer[6], sizeof(U8));
        returnVal->autoSample = (Boolean)((prepare2 >> 0) & 0x01);
    }
    /*************************/

    return result;
}

ErrorCode tctlmCubeNodeControlProgramLitefufors1_getNodeSvcSettings(TypeDef_TctlmEndpoint *endpoint, Boolean *returnVal)
{
    ErrorCode result = CUBEOBC_ERROR_OK;
    TctlmCommsMasterSvc_Endpoint target;
    U8 *buffer;
    U32 bufferSize;
    U32 bufferSizeUsed;

    ZERO_VAR(target);

    target.id = 192;
    MEMCPY((U8 *)&target.endpoint, (U8 *)endpoint, sizeof(TypeDef_TctlmEndpoint));

    buffer = cubeObc_tctlmCommsMasterSvc_buffer(&target);
    bufferSize = cubeObc_tctlmCommsMasterSvc_bufferSize(&target);

    if (bufferSize < 1)
    {
        result = CUBEOBC_ERROR_SIZEL;
    }

    if (result == CUBEOBC_ERROR_OK)
    {
        bufferSizeUsed = 0;
        result = cubeObc_tctlmCommsMasterSvc_sendReceive(&target, &bufferSizeUsed);
    }

    if (result == CUBEOBC_ERROR_OK)
    {
        if (bufferSizeUsed != 1)
        {
            result = CUBEOBC_ERROR_TLM_SIZE;
        }
    }

    if (result == CUBEOBC_ERROR_OK)
    {
        U8 prepare0;
        MEMCPY(&prepare0, &buffer[0], sizeof(U8));
        *returnVal = (Boolean)((prepare0 >> 0) & 0x01);
    }
    /*************************/

    return result;
}

ErrorCode tctlmCubeNodeControlProgramLitefufors1_getUforsPortMap(TypeDef_TctlmEndpoint *endpoint, TctlmCubeNodeControlProgramLitefufors1_UforsPortMap *returnVal)
{
    ErrorCode result = CUBEOBC_ERROR_OK;
    TctlmCommsMasterSvc_Endpoint target;
    U8 *buffer;
    U32 bufferSize;
    U32 bufferSizeUsed;

    ZERO_VAR(target);

    target.id = 193;
    MEMCPY((U8 *)&target.endpoint, (U8 *)endpoint, sizeof(TypeDef_TctlmEndpoint));

    buffer = cubeObc_tctlmCommsMasterSvc_buffer(&target);
    bufferSize = cubeObc_tctlmCommsMasterSvc_bufferSize(&target);

    if (bufferSize < 1)
    {
        result = CUBEOBC_ERROR_SIZEL;
    }

    if (result == CUBEOBC_ERROR_OK)
    {
        bufferSizeUsed = 0;
        result = cubeObc_tctlmCommsMasterSvc_sendReceive(&target, &bufferSizeUsed);
    }

    if (result == CUBEOBC_ERROR_OK)
    {
        if (bufferSizeUsed != 1)
        {
            result = CUBEOBC_ERROR_TLM_SIZE;
        }
    }

    if (result == CUBEOBC_ERROR_OK)
    {
        U8 prepare0;
        MEMCPY(&prepare0, &buffer[0], sizeof(U8));
        returnVal->uforsAxisPort1 = (TctlmCubeNodeControlProgramLitefufors1_UforsAxis)((prepare0 >> 0) & 0x03);
        if ((U8)returnVal->uforsAxisPort1 > 3)
        {
            result = CUBEOBC_ERROR_PARAM;
        }
    }
    /*************************/
    if (result == CUBEOBC_ERROR_OK)
    {
        U8 prepare1;
        MEMCPY(&prepare1, &buffer[0], sizeof(U8));
        returnVal->uforsAxisPort2 = (TctlmCubeNodeControlProgramLitefufors1_UforsAxis)((prepare1 >> 2) & 0x03);
        if ((U8)returnVal->uforsAxisPort2 > 3)
        {
            result = CUBEOBC_ERROR_PARAM;
        }
    }
    /*************************/
    if (result == CUBEOBC_ERROR_OK)
    {
        U8 prepare2;
        MEMCPY(&prepare2, &buffer[0], sizeof(U8));
        returnVal->uforsAxisPort3 = (TctlmCubeNodeControlProgramLitefufors1_UforsAxis)((prepare2 >> 4) & 0x03);
        if ((U8)returnVal->uforsAxisPort3 > 3)
        {
            result = CUBEOBC_ERROR_PARAM;
        }
    }
    /*************************/
    if (result == CUBEOBC_ERROR_OK)
    {
        U8 prepare3;
        MEMCPY(&prepare3, &buffer[0], sizeof(U8));
        returnVal->uforsAxisPort4 = (TctlmCubeNodeControlProgramLitefufors1_UforsAxis)((prepare3 >> 6) & 0x03);
        if ((U8)returnVal->uforsAxisPort4 > 3)
        {
            result = CUBEOBC_ERROR_PARAM;
        }
    }
    /*************************/

    return result;
}

ErrorCode tctlmCubeNodeControlProgramLitefufors1_getNodeSvcStatus(TypeDef_TctlmEndpoint *endpoint, TctlmCubeNodeControlProgramLitefufors1_NodeSvcStatus *returnVal)
{
    ErrorCode result = CUBEOBC_ERROR_OK;
    TctlmCommsMasterSvc_Endpoint target;
    U8 *buffer;
    U32 bufferSize;
    U32 bufferSizeUsed;

    ZERO_VAR(target);

    target.id = 194;
    MEMCPY((U8 *)&target.endpoint, (U8 *)endpoint, sizeof(TypeDef_TctlmEndpoint));

    buffer = cubeObc_tctlmCommsMasterSvc_buffer(&target);
    bufferSize = cubeObc_tctlmCommsMasterSvc_bufferSize(&target);

    if (bufferSize < 13)
    {
        result = CUBEOBC_ERROR_SIZEL;
    }

    if (result == CUBEOBC_ERROR_OK)
    {
        bufferSizeUsed = 0;
        result = cubeObc_tctlmCommsMasterSvc_sendReceive(&target, &bufferSizeUsed);
    }

    if (result == CUBEOBC_ERROR_OK)
    {
        if (bufferSizeUsed != 13)
        {
            result = CUBEOBC_ERROR_TLM_SIZE;
        }
    }

    if (result == CUBEOBC_ERROR_OK)
    {
        U8 prepare0;
        MEMCPY(&prepare0, &buffer[0], sizeof(U8));
        returnVal->nodeState = (TctlmCubeNodeControlProgramLitefufors1_NodeState)((prepare0 >> 0) & 0xFF);
        if ((U8)returnVal->nodeState > 2)
        {
            result = CUBEOBC_ERROR_PARAM;
        }
    }
    /*************************/
    if (result == CUBEOBC_ERROR_OK)
    {
        MEMCPY(&returnVal->bootErrorCodeUforsX, &buffer[1], sizeof(U32));
    }
    /*************************/
    if (result == CUBEOBC_ERROR_OK)
    {
        MEMCPY(&returnVal->bootErrorCodeUforsY, &buffer[5], sizeof(U32));
    }
    /*************************/
    if (result == CUBEOBC_ERROR_OK)
    {
        MEMCPY(&returnVal->bootErrorCodeUforsZ, &buffer[9], sizeof(U32));
    }
    /*************************/

    return result;
}

ErrorCode tctlmCubeNodeControlProgramLitefufors1_getUforsTlm(TypeDef_TctlmEndpoint *endpoint, TctlmCubeNodeControlProgramLitefufors1_UforsTlm *returnVal)
{
    ErrorCode result = CUBEOBC_ERROR_OK;
    TctlmCommsMasterSvc_Endpoint target;
    U8 *buffer;
    U32 bufferSize;
    U32 bufferSizeUsed;

    ZERO_VAR(target);

    target.id = 195;
    MEMCPY((U8 *)&target.endpoint, (U8 *)endpoint, sizeof(TypeDef_TctlmEndpoint));

    buffer = cubeObc_tctlmCommsMasterSvc_buffer(&target);
    bufferSize = cubeObc_tctlmCommsMasterSvc_bufferSize(&target);

    if (bufferSize < 15)
    {
        result = CUBEOBC_ERROR_SIZEL;
    }

    if (result == CUBEOBC_ERROR_OK)
    {
        bufferSizeUsed = 0;
        result = cubeObc_tctlmCommsMasterSvc_sendReceive(&target, &bufferSizeUsed);
    }

    if (result == CUBEOBC_ERROR_OK)
    {
        if (bufferSizeUsed != 15)
        {
            result = CUBEOBC_ERROR_TLM_SIZE;
        }
    }

    if (result == CUBEOBC_ERROR_OK)
    {
        F32 prepare0;
        MEMCPY(&prepare0, &buffer[0], sizeof(F32));
        if ((prepare0 < -20) || (prepare0 > 20))
        {
            result = CUBEOBC_ERROR_PARAM;
        }
        returnVal->rateUforsX = prepare0;
    }
    /*************************/
    if (result == CUBEOBC_ERROR_OK)
    {
        F32 prepare1;
        MEMCPY(&prepare1, &buffer[4], sizeof(F32));
        if ((prepare1 < -20) || (prepare1 > 20))
        {
            result = CUBEOBC_ERROR_PARAM;
        }
        returnVal->rateUforsY = prepare1;
    }
    /*************************/
    if (result == CUBEOBC_ERROR_OK)
    {
        F32 prepare2;
        MEMCPY(&prepare2, &buffer[8], sizeof(F32));
        if ((prepare2 < -20) || (prepare2 > 20))
        {
            result = CUBEOBC_ERROR_PARAM;
        }
        returnVal->rateUforsZ = prepare2;
    }
    /*************************/
    if (result == CUBEOBC_ERROR_OK)
    {
        U8 prepare3;
        MEMCPY(&prepare3, &buffer[12], sizeof(U8));
        returnVal->nogoUforsX = (Boolean)((prepare3 >> 0) & 0x01);
    }
    /*************************/
    if (result == CUBEOBC_ERROR_OK)
    {
        U8 prepare4;
        MEMCPY(&prepare4, &buffer[12], sizeof(U8));
        returnVal->rstAckUforsX = (Boolean)((prepare4 >> 1) & 0x01);
    }
    /*************************/
    if (result == CUBEOBC_ERROR_OK)
    {
        U8 prepare5;
        MEMCPY(&prepare5, &buffer[12], sizeof(U8));
        returnVal->tempWarnUforsX = (Boolean)((prepare5 >> 2) & 0x01);
    }
    /*************************/
    if (result == CUBEOBC_ERROR_OK)
    {
        U8 prepare6;
        MEMCPY(&prepare6, &buffer[12], sizeof(U8));
        returnVal->auxControlLoopErrUforsX = (Boolean)((prepare6 >> 3) & 0x01);
    }
    /*************************/
    if (result == CUBEOBC_ERROR_OK)
    {
        U8 prepare7;
        MEMCPY(&prepare7, &buffer[12], sizeof(U8));
        returnVal->hardBitErrUforsX = (Boolean)((prepare7 >> 4) & 0x01);
    }
    /*************************/
    if (result == CUBEOBC_ERROR_OK)
    {
        U8 prepare8;
        MEMCPY(&prepare8, &buffer[12], sizeof(U8));
        returnVal->measRangeErrUforsX = (Boolean)((prepare8 >> 5) & 0x01);
    }
    /*************************/
    if (result == CUBEOBC_ERROR_OK)
    {
        U8 prepare9;
        MEMCPY(&prepare9, &buffer[12], sizeof(U8));
        returnVal->unknwnCmdUforsX = (Boolean)((prepare9 >> 6) & 0x01);
    }
    /*************************/
    if (result == CUBEOBC_ERROR_OK)
    {
        U8 prepare10;
        MEMCPY(&prepare10, &buffer[12], sizeof(U8));
        returnVal->nogoUforsY = (Boolean)((prepare10 >> 7) & 0x01);
    }
    /*************************/
    if (result == CUBEOBC_ERROR_OK)
    {
        U8 prepare11;
        MEMCPY(&prepare11, &buffer[13], sizeof(U8));
        returnVal->rstAckUforsY = (Boolean)((prepare11 >> 0) & 0x01);
    }
    /*************************/
    if (result == CUBEOBC_ERROR_OK)
    {
        U8 prepare12;
        MEMCPY(&prepare12, &buffer[13], sizeof(U8));
        returnVal->tempWarnUforsY = (Boolean)((prepare12 >> 1) & 0x01);
    }
    /*************************/
    if (result == CUBEOBC_ERROR_OK)
    {
        U8 prepare13;
        MEMCPY(&prepare13, &buffer[13], sizeof(U8));
        returnVal->auxControlLoopErrUforsY = (Boolean)((prepare13 >> 2) & 0x01);
    }
    /*************************/
    if (result == CUBEOBC_ERROR_OK)
    {
        U8 prepare14;
        MEMCPY(&prepare14, &buffer[13], sizeof(U8));
        returnVal->hardBitErrUforsY = (Boolean)((prepare14 >> 3) & 0x01);
    }
    /*************************/
    if (result == CUBEOBC_ERROR_OK)
    {
        U8 prepare15;
        MEMCPY(&prepare15, &buffer[13], sizeof(U8));
        returnVal->measRangeErrUforsY = (Boolean)((prepare15 >> 4) & 0x01);
    }
    /*************************/
    if (result == CUBEOBC_ERROR_OK)
    {
        U8 prepare16;
        MEMCPY(&prepare16, &buffer[13], sizeof(U8));
        returnVal->unknwnCmdUforsY = (Boolean)((prepare16 >> 5) & 0x01);
    }
    /*************************/
    if (result == CUBEOBC_ERROR_OK)
    {
        U8 prepare17;
        MEMCPY(&prepare17, &buffer[13], sizeof(U8));
        returnVal->nogoUforsZ = (Boolean)((prepare17 >> 6) & 0x01);
    }
    /*************************/
    if (result == CUBEOBC_ERROR_OK)
    {
        U8 prepare18;
        MEMCPY(&prepare18, &buffer[13], sizeof(U8));
        returnVal->rstAckUforsZ = (Boolean)((prepare18 >> 7) & 0x01);
    }
    /*************************/
    if (result == CUBEOBC_ERROR_OK)
    {
        U8 prepare19;
        MEMCPY(&prepare19, &buffer[14], sizeof(U8));
        returnVal->tempWarnUforsZ = (Boolean)((prepare19 >> 0) & 0x01);
    }
    /*************************/
    if (result == CUBEOBC_ERROR_OK)
    {
        U8 prepare20;
        MEMCPY(&prepare20, &buffer[14], sizeof(U8));
        returnVal->auxControlLoopErrUforsZ = (Boolean)((prepare20 >> 1) & 0x01);
    }
    /*************************/
    if (result == CUBEOBC_ERROR_OK)
    {
        U8 prepare21;
        MEMCPY(&prepare21, &buffer[14], sizeof(U8));
        returnVal->hardBitErrUforsZ = (Boolean)((prepare21 >> 2) & 0x01);
    }
    /*************************/
    if (result == CUBEOBC_ERROR_OK)
    {
        U8 prepare22;
        MEMCPY(&prepare22, &buffer[14], sizeof(U8));
        returnVal->measRangeErrUforsZ = (Boolean)((prepare22 >> 3) & 0x01);
    }
    /*************************/
    if (result == CUBEOBC_ERROR_OK)
    {
        U8 prepare23;
        MEMCPY(&prepare23, &buffer[14], sizeof(U8));
        returnVal->unknwnCmdUforsZ = (Boolean)((prepare23 >> 4) & 0x01);
    }
    /*************************/

    return result;
}

ErrorCode tctlmCubeNodeControlProgramLitefufors1_getUforsTlmY(TypeDef_TctlmEndpoint *endpoint, TctlmCubeNodeControlProgramLitefufors1_UforsTlmY *returnVal)
{
    ErrorCode result = CUBEOBC_ERROR_OK;
    TctlmCommsMasterSvc_Endpoint target;
    U8 *buffer;
    U32 bufferSize;
    U32 bufferSizeUsed;

    ZERO_VAR(target);

    target.id = 196;
    MEMCPY((U8 *)&target.endpoint, (U8 *)endpoint, sizeof(TypeDef_TctlmEndpoint));

    buffer = cubeObc_tctlmCommsMasterSvc_buffer(&target);
    bufferSize = cubeObc_tctlmCommsMasterSvc_bufferSize(&target);

    if (bufferSize < 5)
    {
        result = CUBEOBC_ERROR_SIZEL;
    }

    if (result == CUBEOBC_ERROR_OK)
    {
        bufferSizeUsed = 0;
        result = cubeObc_tctlmCommsMasterSvc_sendReceive(&target, &bufferSizeUsed);
    }

    if (result == CUBEOBC_ERROR_OK)
    {
        if (bufferSizeUsed != 5)
        {
            result = CUBEOBC_ERROR_TLM_SIZE;
        }
    }

    if (result == CUBEOBC_ERROR_OK)
    {
        F32 prepare0;
        MEMCPY(&prepare0, &buffer[0], sizeof(F32));
        if ((prepare0 < -20) || (prepare0 > 20))
        {
            result = CUBEOBC_ERROR_PARAM;
        }
        returnVal->rate = prepare0;
    }
    /*************************/
    if (result == CUBEOBC_ERROR_OK)
    {
        U8 prepare1;
        MEMCPY(&prepare1, &buffer[4], sizeof(U8));
        returnVal->nogo = (Boolean)((prepare1 >> 0) & 0x01);
    }
    /*************************/
    if (result == CUBEOBC_ERROR_OK)
    {
        U8 prepare2;
        MEMCPY(&prepare2, &buffer[4], sizeof(U8));
        returnVal->rstAck = (Boolean)((prepare2 >> 1) & 0x01);
    }
    /*************************/
    if (result == CUBEOBC_ERROR_OK)
    {
        U8 prepare3;
        MEMCPY(&prepare3, &buffer[4], sizeof(U8));
        returnVal->tempWarn = (Boolean)((prepare3 >> 2) & 0x01);
    }
    /*************************/
    if (result == CUBEOBC_ERROR_OK)
    {
        U8 prepare4;
        MEMCPY(&prepare4, &buffer[4], sizeof(U8));
        returnVal->auxControlLoopErr = (Boolean)((prepare4 >> 3) & 0x01);
    }
    /*************************/
    if (result == CUBEOBC_ERROR_OK)
    {
        U8 prepare5;
        MEMCPY(&prepare5, &buffer[4], sizeof(U8));
        returnVal->hardBitErr = (Boolean)((prepare5 >> 4) & 0x01);
    }
    /*************************/
    if (result == CUBEOBC_ERROR_OK)
    {
        U8 prepare6;
        MEMCPY(&prepare6, &buffer[4], sizeof(U8));
        returnVal->measRangeErr = (Boolean)((prepare6 >> 5) & 0x01);
    }
    /*************************/
    if (result == CUBEOBC_ERROR_OK)
    {
        U8 prepare7;
        MEMCPY(&prepare7, &buffer[4], sizeof(U8));
        returnVal->unknwnCmd = (Boolean)((prepare7 >> 6) & 0x01);
    }
    /*************************/

    return result;
}

